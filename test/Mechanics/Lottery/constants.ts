import { ZeroAddress } from "ethers";

import { TokenType } from "../../types";

export const amount = 31429n;

export const price = {
  tokenType: TokenType.NATIVE,
  token: ZeroAddress,
  tokenId: 0n,
  amount: amount
};
